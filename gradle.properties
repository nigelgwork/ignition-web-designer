# Ignition Module Signing Configuration
# Self-signed certificates for Gaskony
#
# ⚠️  SECURITY WARNING ⚠️
# These are DEVELOPMENT-ONLY credentials with PUBLIC passwords.
# DO NOT use these certificates for production deployments!
#
# For production:
# 1. Generate new certificates with private keys
# 2. Store passwords in CI/CD secrets or environment variables
# 3. NEVER commit production credentials to version control
#
# These development certificates are kept in git for:
# - Reproducible builds across environments
# - CI/CD compatibility without extra configuration
# - Immediate clone-and-build capability

ignition.signing.keystoreFile=keystore.jks
ignition.signing.keystorePassword=gaskony2024
ignition.signing.certFile=certificate.der
ignition.signing.certAlias=gaskony
ignition.signing.certPassword=gaskony2024
